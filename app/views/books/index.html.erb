    <h1><%= notice %></h1>

    <table border="1">
        <tr>
        <% no=1  %>
            <td> no </td>
            <td> judul </td>
            <td> harga </td>
            <td> halaman </td>
            <td> deskripsi </td>
            <td>aksi </td>
        </tr>
        <%  @books.each do |book| %>
        <tr>
            <td> <%= no %> </td>
            <td> <%= book.title %> </td>
            <td> <%= book.price %> </td>
            <td> <%= book.page %> </td>
            <td> <%= book.description %> </td>
            <td> <%= link_to "edit"  ,edit_book_path(book) %> </td>
        </tr>
        <% no+=1 end %>  
    </table>
